// 메인 페이지
import logo from './logo.svg';
import './App.css'; // 현재 컴포넌트에서 사용할 css 파일 불러오기


function App() {
  // 변수 생성
  let title="강남 우동 맛집"
  return (
    <div className="App">
      {/* 블로그 레이아웃 */}
      {/* 상단 메뉴 */}
      {/* class 속성 설정 : className */}
      <div className="black-nav">
        <h4 style={ { color: 'red', fontSize: '16px' } }>블로그</h4>
      </div>
      {/* 가져온 데이터 사용(데이터 바인딩) */}
      <h4>{ title }</h4>
    </div>
  );
}

export default App;

